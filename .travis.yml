install:
  - pip install -r requirements.txt
addons:
  apt:
    packages:
      - sshpass
language: python
python:
  - 3.6
script:
  - python -m pytest -v
  - flake8
before_deploy:
  - openssl aes-256-cbc -d -K $REPO_ENC_KEY -iv $REPO_ENC_IV -in deploy_key.enc -out /tmp/deploy_key
  # Make sure only the current user can read the private key
  - chmod 600 /tmp/deploy_key
  - chmod +x ./deploy.sh
  # Create a script to return the passphrase environment variable to ssh-add
#  - echo 'echo ${SSL_PASSPHRASE}' > /tmp/askpass && chmod +x /tmp/askpass
  # Start the authentication agent
  - eval "$(ssh-agent -s)"
  # Add the key to the authentication agent
#  - DISPLAY=":0.0" SSH_ASKPASS="/tmp/askpass" setsid ssh-add /tmp/deploy_key </dev/null
  - echo -e "Host $SERVER_IP_ADDRESS\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
#  - bash ssh-password.sh
  - sshpass -p travis ssh -i /tmp/deploy_key ubuntu@35.158.109.210 pwd
deploy:
  provider: script
  skip_cleanup: true
  script: sshpass -p travis ssh -i /tmp/deploy_key ubuntu@35.158.109.210 'bash ./deploy.sh'
  on:
    branch: master